# Cargo audit configuration
# See: https://rustsec.org/advisories/

[advisories]
# Temporarily ignore pyo3 buffer overflow vulnerability
# Upgrade to pyo3 >= 0.24.1 is pending (requires pyo3-asyncio migration)
# Tracked: RUSTSEC-2025-0020
ignore = ["RUSTSEC-2025-0020"]

# Informational warnings to show (unmaintained, unsound, notice, yanked)
informational_warnings = ["unmaintained", "unsound"]
